# Contributing

1. Branch off `main` using the format `feature/description` or `fix/description`.
2. Ensure all TypeScript errors are resolved (`npm run build` or `npx tsc --noEmit`).
3. Create a PR and request review.
4. Do not merge your own PRs.