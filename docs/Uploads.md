# Uploads (UploadThing)

We use UploadThing for document and image storage.

## Configuration
Defined in `src/app/api/uploadthing/core.ts`. We enforce strict file size and type limits.
Requires `UPLOADTHING_SECRET` and `UPLOADTHING_APP_ID`.